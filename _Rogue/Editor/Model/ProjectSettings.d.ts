/**
 * Defines the data structore of the project settings.
 * A ProjectSettings object contains the preferences for a specific project.
 */
export default class ProjectSettings {
    private _title;
    private _author;
    private _email;
    private _path;
    private _editorLayout;
    private _resolution;
    private _quality;
    private _inputLayout;
    private _minify;
    private _scenesMap;
    title: string;
}
