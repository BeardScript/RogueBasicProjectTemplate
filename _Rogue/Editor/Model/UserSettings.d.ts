/**
 * Defines the model of our user settings.
 * A UserSettings object contains the preferences of the editor for all projects for a specific user.
 */
export default class UserSettings {
    private _editorLayout;
    private _defaultCodeEditor;
}
